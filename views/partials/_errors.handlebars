{{#if error}}
<div class="container">
  <div class="container alert alert-danger">{{error}}</div>
</div>

{{/if}}

{{#each errors}}
<div class="container">
  <div class="container alert alert-danger">{{text}}</div>
</div>

{{else}}

{{/each}}
